import 'package:flutter/material.dart';

// colors
const kPrimaryColor = Color(0xff1C161E);
const kSecondaryColor = Color(0xffEFE3C8);
const kContainerColor = Color(0xff414141);
const kOverlayColor = Color(0xff27181D);

// Timers
const kTransitionDuration = Duration(milliseconds: 1000);

const String testingParagraph =
    'A single espresso shot poured into hot foamy milk, with the surface topped with mildly sweetened cocoa powder and drizzled with scrumptious chocolate sauce. This is the perfect way to start your day. ';
